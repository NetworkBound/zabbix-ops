# Exported templates land here (scripts/templates.py export).
#
# The YAML is gitignored by default: a template export describes your own
# estate, and publishing it is a decision you should make deliberately rather
# than by forgetting to look. Remove the ignore rule in .gitignore once you
# have checked what is in yours.
